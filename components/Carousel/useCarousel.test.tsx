import { renderHook } from "@testing-library/react";
import { useCarousel } from "./useCarousel";

describe("useCarousel", () => {
  it("should have ", () => {
    renderHook;
    useCarousel();
  });
});
