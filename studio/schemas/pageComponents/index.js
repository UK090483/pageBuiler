import listing from "./listing";
import Section from "./Section";
import defaultRichText from "./defaultRichText";

const PageComponents = [Section, listing, defaultRichText];

export default PageComponents;
